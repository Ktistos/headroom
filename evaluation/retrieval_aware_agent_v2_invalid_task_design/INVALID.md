# Invalid low-regime task design

This complete raw matrix is retained for audit but excluded from aggregate claims. The original low-retrieval tasks assumed array positions 0 and 1 survived lossy sampling; live traces disproved that assumption, and lossy answers failed the hidden grader. One high historical run also exceeded the API-request guard, and the controlled passthrough row exposed an outer-gate event-recording gap. The replacement matrix uses a schema question answerable from every sampled row and records rejected compression candidates as the passthrough action actually emitted.
