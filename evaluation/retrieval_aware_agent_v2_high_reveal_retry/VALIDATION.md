# Validation note

All three delayed-reveal beta rows used fresh seed `2927455522424617784`, passed the hidden grader, and passed every benchmark validity check. Historical behavior (observe), Always lossless, and Retrieval-aware were rerun symmetrically against the same generated input and prompt. All three are included in the validated matrix.
